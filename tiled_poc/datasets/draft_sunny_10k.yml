# AUTO-GENERATED by broker.inspect on 2026-02-20
# Source: /sdf/data/lcls/ds/prj/prjmaiqmag01/results/data-source/data-tlinker/SUNNY/NIPS_10000
# Files scanned: 1 HDF5 (NiPS_10000.h5)
# Entities detected: 10,000 (1 files x 10,000 batch size)

# === REQUIRED: Fill in these identity fields ===
label: ""           # TODO: dataset name (e.g., my_simulation)
key_prefix: ""      # TODO: short prefix for Tiled keys (e.g., sim)

# === Auto-detected ===
data:
  directory: /sdf/data/lcls/ds/prj/prjmaiqmag01/results/data-source/data-tlinker/SUNNY/NIPS_10000
  file_pattern: "NiPS_10000.h5"
  layout: batched
  # batch_size: 10000

parameters:
  location: group
  group: /params
  # 9 parameters discovered:
  #   Ax                float64  range [-0.02, -0.0]
  #   Az                float64  range [0.0, 0.41986]
  #   J1a               float64  range [-5.4, -0.00056]
  #   J1b               float64  range [-3.99932, -5e-05]
  #   J2a               float64  range [2e-05, 0.39999]
  #   J2b               float64  range [1e-05, 0.39996]
  #   J3a               float64  range [0.00318, 27.79655]
  #   J3b               float64  range [0.00215, 27.79782]
  #   J4                float64  range [-0.75991, -0.00014]

# === TODO: Confirm artifact classification ===
# These datasets have shape (batch, ...) with ndim > 1 → classified as artifacts
artifacts:
  - type: data           # TODO: rename if desired
    dataset: /data
    # shape per entity: (384, 384), dtype: float64, range: [0.0, 3.469452445889711]

# === TODO: Confirm shared axes ===
# These datasets do NOT have the batch dimension → classified as shared
shared:
  - type: E_axis
    dataset: /E_axis
    # shape: (384,), monotonic, range [0.0, 150.0], step=0.3916
  - type: Q_axis
    dataset: /Q_axis
    # shape: (384, 3), range [0.0, 3.0]

# === Additional per-entity data (not under params/) ===
# TODO: Keep as metadata, promote to parameter, or remove?
extra_metadata:
  - dataset: /Exp Parameters
    # shape per entity: (), dtype: bool

# === Optional: Add project metadata ===
# project: ""          # scientific project name
# generator: ""        # simulation code
# material: ""         # physical system

# === No provenance attributes found in HDF5 ===
# provenance: {}

# === Recommendations for data producer ===
# - No 'created_at' timestamp — add as HDF5 root attribute
# - No 'generator' or 'code_version' — add as HDF5 root attribute
# - No 'material' identifier — add as HDF5 root attribute

# === Consistency check: PASSED (1 files checked) ===
